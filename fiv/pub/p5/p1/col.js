function crCol(){

Col = [color('#00aeef'), color('#ec008c'), color('#72c8b6'),color("blue")];
  for (let i = 0; i < 200; i++) {
    fet[i] = new Fet(random(0, width), random(-height, 0), random(-1, 1));
  }

}

